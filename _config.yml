# GitHub Pages Configuration
# This file is kept for reference but Jekyll is no longer used.
# Documentation is now built with VitePress and deployed via GitHub Actions.
#
# To preview locally:
#   cd docs && npm install && npm run dev
#
# To build for production:
#   cd docs && npm install && npm run build

title: Note Sync Now (VitePress)
description: Documentation now built with VitePress - See docs/ directory

# Disable Jekyll builds - we use VitePress now
exclude:
  - "*"
