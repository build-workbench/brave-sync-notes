name: Deploy to GitHub Pages

# This workflow now triggers the docs workflow
on:
  push:
    branches: [master, main]
  workflow_dispatch:

# Just trigger the docs workflow
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Docs Deployment
        run: echo "Documentation is deployed via docs.yml workflow"
